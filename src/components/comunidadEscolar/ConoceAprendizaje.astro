---
import { personas as items } from "../../data/personas"

---

<div class="bg-redGRG w-full">
    <div class="container mx-auto pt-12 pb-[18rem] mb-[30rem] relative">
        <div class="w-full lg:w-[50%]">
            <h2 class="text-64 text-center font-semibold text-white">Conoce a quienes hacen posible tu aprendizaje</h2>
        </div>
        <p class="text-center text-20 text-white mt-6 mb-4">Lorem ipsum dolor sit amet consectetur adipisicing elit. Nesciunt, modi.</p>
        <div class="container absolute left-0 lg:left-auto">
            <div class="splide" id="splideTutores" role="group" aria-label="Splide Basic HTML Example">
                <div class="splide__track">                        
                        <ul class="splide__list">                            
                            
                            {
                                items.map((item) => (
                                    <li class="splide__slide">
                                        <div class="bg-grayExtra rounded-lg overflow-hidden bg-gray-300 space-y-6">
                                            <img class="w-full h-[20rem] object-cover" src={item.image} alt="">
                                            <div class="p-4">
                                                <p class="text-30 font-semibold text-lightGrayGRG">Nombre</p>
                                                <p class="text-20 font-semibold">Lic. Admin</p>
                                                <p class="text-xl"><span class="text-yellow-400 text-45 font-semibold">4+ </span> / Años de experiencia</p>
                                            </div>
                                        </div>
                                    </li>
                                ))
                            }
                        </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    import "@splidejs/splide"
    import '@splidejs/splide/css';

    import Splide from "@splidejs/splide";

    let splideTutores= new Splide("#splideTutores", {
        type: "loop",
        gap: "5rem",
        perPage: 3,
        // arrows: true,
        // focus: "center",
        classes: {
            pagination: 'splide__pagination paginationTutores',
            page      : 'splide__pagination__page your-class-page',
    },
    breakpoints: {
        1100:{
            perPage: 2,
        },
		800: {
			perPage: 1,
		},
        
   }
        // pagination: 'slider'
    }).mount();
        
    // let btnnoticiasLeft = document.getElementById('btnnoticiasLeft')
    //     btnnoticiasLeft.addEventListener('click', e => {
    //     splideNoticias.go('-1')

        
    // })

    // let btnnoticiasRight = document.getElementById('btnnoticiasRight')
    //     btnnoticiasRight.addEventListener('click', e => {
    //     splideNoticias.go('+1')

        
    // })


</script>